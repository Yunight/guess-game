export const getLegendaryMasterTitle = (language: string): string =>
	language === "fr" ? "MAÎTRE POKÉMON LÉGENDAIRE!" : "LEGENDARY POKÉMON MASTER!";

export const getGenerationCompletedSubtitle = (language: string): string =>
	language === "fr" ? "Génération Complétée!" : "Generation Completed!";
